dbrp "telegraf"."autogen"

stream
    |from()
        .measurement('cpu')
        .groupBy('host')
    |alert()
        .crit(lambda: "usage_idle" < 60)
        .warn(lambda: "usage_idle" < 70)
        .stateChangesOnly()
        .topic('cpu')
